# PulseCraft PPG - Python dependencies (Python 3.11+)
# Lower bounds only; newer versions are expected to work.
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
pyserial>=3.5        # wired Arduino serial
PyQt6>=6.5           # collection / analysis / live GUIs
pyqtgraph>=0.13      # live plotting
Flask>=2.3           # the small app.py launcher pages

# tools/ppg_simulator.py and tools/check_setup.py need only numpy + the standard library.
